<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mine.c66bridge">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Broadcast Receiver for Chainway C66 RFID & Barcode Scanner Intents -->
        <receiver android:name=".ScanReceiver" android:exported="true">
            <intent-filter>
                <action android:name="com.scanner.broadcast" />
                <action android:name="android.intent.ACTION_DECODE_DATA" />
                <action android:name="com.rsc.scan.action" />
                <action android:name="com.rsc.action.UHF_RECEIVE_DATA" />
                <action android:name="com.android.server.scannerservice.broadcast" />
                <action android:name="com.rfid.KEY_DOWN" />
                <action android:name="android.rfid.FUN_KEY" />
                <action android:name="nl.chainway.rfid.TAG_READ" />
                <action android:name="com.ubx.datawedge.TAG_DATA" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
